= image_tag @article.image_url
= @article.book_title
= @article.author
= @article.title
= @article.content
= link_to account_path(@article.user) do
  = image_tag @article.user.avatar_image, class: 'icon_mini'

- if user_signed_in?
  - if current_user.has_written?(@article)
    = link_to '編集する', edit_article_path(@article), class: 'text-decoration'
    = link_to '削除する', article_path(@article),class: 'text-decoration' , data: {method: 'delete', confirm: '本当に削除してもよろしいですか'}
  - else
    - if current_user.has_liked?(@article)
      .heart
        = link_to article_likes_path(@article), data: {method: 'delete'} do
          = image_tag 'heart-active.svg'
    - else
      .heart
        = link_to article_likes_path(@article), data: { method: 'post' } do
          = image_tag 'heart.svg'
